[package]
name = "genaryx-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish = false
description = "Genaryx core: all console logic (ingest, store, conform, commands, signing, evidence)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
jsonschema = { workspace = true }
chrono = { workspace = true }
rusqlite = { workspace = true }
tokio = { workspace = true }
# Phase-4 wave 3 (crates/core/src/evidence.rs): the Evidence Center hashes each
# pack artifact (manifest integrity) and assembles the pack into one zip.
# `sha2` is already the workspace hash (genaryx-signing uses the same pin), so
# this is a new direct edge to an already-resolved crate.
sha2 = { workspace = true }
zip = { workspace = true }
