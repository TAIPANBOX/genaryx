[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
# Host target (aarch64-apple-darwin on the dev box) is always available; add
# windows/linux cross targets in later phases when the Tauri shell ships there.
