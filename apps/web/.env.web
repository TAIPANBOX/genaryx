# The browser build talks to a genaryx-web on the SAME origin it was served
# from, so the console and its backend are one thing to reach over the tunnel
# and there is no second host to allow, no CORS, and no cross-origin cookie.
VITE_GENARYX_API=/api
